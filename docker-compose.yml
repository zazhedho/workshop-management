version: '3.8'

services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - DATABASE_URL='postgresql://neondb_owner:npg_Jmg9VcCk2NBG@ep-floral-night-a1brxo2e-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require'
    volumes:
      - ./migrations:/migrations
